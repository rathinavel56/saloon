import { TargetEntityToNamePipe } from './target-entity-to-name.pipe';

describe('TargetEntityToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TargetEntityToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
